# This file is managed by AFT - do not modify
terraform {
  backend "s3" {
    bucket         = "{{ bucket }}"
    key            = "{{ key }}"
    region         = "{{ region }}"
    dynamodb_table = "{{ dynamodb_table }}"
    kms_key_id     = "{{ kms_key_id }}"
    encrypt        = true
  }
}

