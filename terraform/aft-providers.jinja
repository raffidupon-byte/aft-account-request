# This file is managed by AFT - do not modify
terraform {
  required_version = ">= 1.2.0"
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = ">= 5.0.0"
    }
  }
}

provider "aws" {
  region = "{{ region }}"
  assume_role {
    role_arn    = "arn:aws:iam::{{ vended_account_id }}:role/AWSAFTExecution"
    session_name = "AFT-Account-Request"
  }
}

